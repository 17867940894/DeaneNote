# DeanNote 知识库使用规则

本目录是 Dean 的 Obsidian 私人学习笔记库（双链 vault）。回答用户问题时，先判断我的知识体系有没有答案，有就继承，没有就补充，并按照以下规则处理：

1. 如果知识库中存在与用户问题直接相关的内容：
   - 优先基于笔记内容进行回答；
   - 将笔记内容作为 Dean 的个人认知、经验和上下文依据；
   - 可以结合模型自身知识进行补充，但不得违背笔记中的明确结论。

2. 如果知识库中没有相关内容：
   - 不强行关联知识库；
   - 直接基于模型已有知识回答；
   - 不要声称参考过不存在的笔记。

3. 如果知识库内容与通用知识存在冲突：
   - 明确指出差异；
   - 优先保留知识库中经过个人沉淀的观点、决策和实践经验；
   - 对客观事实类问题，以可靠事实为准。

4. 回答时应区分：
   - 【知识库依据】：来自 Dean 笔记中的信息；
   - 【通用知识补充】：模型已有知识推理；
   - 【综合结论】：结合两者后的回答。
   
   目标：
   让回答逐渐符合 Dean 的知识体系、工作方式和长期积累，而不是每次重新生成一个独立答案。

## 笔记查阅方式（当前无本地语义检索）

> 本机已卸载 Ollama 与 RAG 模型，不再依赖 obsidian-rag 等本地语义检索。
> 如需核对 vault 内具体笔记，直接用 Grep / Read 工具查阅对应的 Markdown 文件即可，
> 无需本地索引或模型推理。

## 技能路由

- 涉及笔记整理 / 双链 / 命名 / 结构优化等任务时，先读根目录 `SKILL.md`（路由），按路由加载 `skill-base/` 下的子技能并按其步骤执行。
- `skill-base/` 只放定制化、高频使用的技能；任务需要额外能力时，到 Agent Base 技能库
  `D:\Agent Base\skills\` 查找匹配技能（读取其 SKILL.md 按步骤执行），找不到再走生态搜索。

## 约束

- 不要编造知识库里没有的内容；笔记里没有记录时明确说"笔记中没有记录"。
- `config.md` 含明文服务器凭据：禁止读取、回显、提交；如发现它出现在知识库同步/检索范围内，
  应提醒用户把它移出 vault。
- 除非用户明确要求，不要删除、重命名或大改 vault 内的笔记。
- **图片/附件存放约定**：粘贴的图片/附件必须进就近的 `assets/` 文件夹（不带点），
  用 `![[...]]` 或 `![](...)` 引用；**禁止使用 `.assets/`（带点）隐藏目录**——
  Obsidian 不索引隐藏目录，图片会失效且可能被子技能"自动删除未链接附件"误删。
