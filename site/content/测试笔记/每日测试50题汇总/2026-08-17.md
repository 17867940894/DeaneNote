# 每日测试卷（2026-08-17）

## 一、单选题（共 25 题）

1. Python 中，可通过标准库的哪个模块查看当前版本的全部保留关键字？
A. sys
B. keyword
C. math
D. random

2. Python 字符串前加 r 前缀（如 r"hello\nworld"）的作用是？
A. 反斜杠不再转义，\n 按原样显示
B. 将字符串转换为字节串
C. 自动去除字符串首尾空白
D. 使字符串支持重复拼接

3. Python3 中，关于长整型 long 与整型 int 的关系，正确的是？
A. long 与 int 是两种独立类型
B. Python3 中 long 被改名并保留
C. long 已并入 int，Python3 只有 int 一种整数类型
D. long 仅用于复数运算

4. 关于 Python re 模块中 re.match 与 re.search 的区别，正确的是？
A. 两者功能完全相同
B. re.match 从字符串起始位置匹配，re.search 扫描整个字符串返回第一个匹配
C. re.search 只匹配字符串起始位置
D. re.match 匹配失败时返回空字符串

5. 正则表达式标志 re.M | re.I 中，re.I 的含义是？
A. 多行匹配
B. 点号匹配换行符
C. 忽略表达式中的空白
D. 忽略大小写

6. Python 关键字 lambda 用于？
A. 创建匿名函数
B. 声明全局变量
C. 抛出异常
D. 定义生成器

7. Python 中，is 与 == 的区别是？
A. 两者完全等价
B. == 用于判断对象身份
C. is 判断对象身份（是否同一对象），== 判断值是否相等
D. is 判断值是否相等

8. MySQL 分组查询（group by）中，select 后允许出现的字段只能是？
A. 表中任意字段
B. 分组字段和聚合函数
C. 只允许聚合函数
D. 只允许分组字段

9. MySQL 中 union 合并查询的默认行为是？
A. 自动去重
B. 不去重
C. 自动排序
D. 只合并第一个查询的结果

10. MySQL 唯一约束（unique）字段的取值特点，正确的是？
A. 不允许出现 null
B. 只允许出现一个 null
C. 允许多个 null，且非 null 值不能重复
D. null 值之间不允许重复

11. 关于 MySQL 外键约束，以下描述正确的是？
A. 外键建立主表上，指向从表主键
B. 外键字段类型可以与主键类型不同
C. 一张表中可以定义多个主键
D. 外键加在从表上，指向主表的主键，字段类型必须一致

12. Linux 中 find / -name "*.log" 命令的作用是？
A. 在 / 目录下按文件名查找 .log 文件
B. 统计 / 目录下 .log 文件的数量
C. 删除 / 目录下所有 .log 文件
D. 查看 .log 文件的内容

13. Linux 中，mkdir -p 参数的作用是？
A. 删除已存在的目录
B. 递归创建多级目录（父目录不存在时一并创建）
C. 设置目录的访问权限
D. 显示目录的完整路径

14. Linux shell 特殊变量中，用于获取脚本参数个数的是？
A. $0
B. $?
C. $*
D. $#

15. Linux shell 中，上一条命令执行成功返回 0、失败返回非 0，用于获取该返回值的特殊变量是？
A. $?
B. $#
C. $n
D. $@

16. Linux 中 cut -d ":" -f 3 命令的作用是？
A. 以 : 分隔并输出第 3 行内容
B. 删除包含 : 的行
C. 以 : 为分隔符剪切并输出第 3 个字段
D. 统计每行 : 出现的次数

17. Git 中，git config --global user.name "xxx" 命令的作用是？
A. 修改远程仓库地址
B. 设置全局提交者用户名
C. 创建新的分支
D. 查看提交历史

18. TCP 连接断开时的四次挥手，第一次挥手发送的报文段是？
A. 主动关闭方发送 FIN
B. 被动关闭方发送 ACK
C. 双方同时发送 SYN
D. 服务器发送 RST

19. 常见服务端口号中，SSH 远程登录默认使用的端口是？
A. 21
B. 23
C. 53
D. 22

20. HTTP 状态码 404 表示？
A. 服务器内部错误
B. 请求未授权
C. 请求的资源不存在
D. 请求方法不被允许

21. HTTP 状态码 304 Not Modified 表示？
A. 资源未修改，可继续使用本地缓存
B. 重定向到新的地址
C. 请求超时
D. 网关错误

22. HTTP 请求方法中，用于对资源进行"局部更新"的方法是？
A. PUT
B. PATCH
C. OPTIONS
D. HEAD

23. IPv6 地址的二进制长度是？
A. 32 位
B. 64 位
C. 96 位
D. 128 位

24. Selenium 中，设置全局隐式等待（元素查找最长等待时间）的方法是？
A. driver.maximize_window()
B. driver.refresh()
C. WebDriverWait(driver, 10)
D. driver.implicitly_wait(10)

25. Selenium 中，通过 <input type="file"> 标签实现文件上传的标准做法是？
A. 点击元素后用系统弹窗手动选择
B. 使用 pyautogui 模拟按键输入路径
C. 直接对元素调用 send_keys() 传入本地文件路径
D. 使用 Alert 类处理上传

---

## 二、多选题（共 10 题）

26. 以下属于 Python 循环控制关键字的有？（多选）
A. for
B. while
C. continue
D. with

27. 关于 Python 字符串，以下说法正确的有？（多选）
A. 字符串是不可变的
B. 可以用 + 拼接、用 * 重复
C. 单引号和双引号使用完全等价
D. 可以修改字符串中的单个字符

28. 关于 Python re 模块，以下说法正确的有？（多选）
A. re.match 从字符串起始位置开始匹配
B. re.search 扫描整个字符串并返回第一个成功的匹配
C. re.sub 用于替换字符串中的匹配项
D. re.match 匹配失败时返回空字符串

29. 关于 MySQL 外键约束与表关系，以下说法正确的有？（多选）
A. 外键建立在从表上，指向主表的主键
B. 外键字段类型必须与主表主键类型一致
C. 多对多关系需要借助中间表存放两个外键
D. 添加外键约束后可以随意删除，不影响数据一致性

30. 关于 MySQL union 合并查询，以下说法正确的有？（多选）
A. union 默认会自动去重
B. union all 不去重
C. 被合并的两个 select 查询结果的列数必须一致
D. 可以先分别排序再合并

31. 关于 Linux 文本处理命令，以下对应正确的有？（多选）
A. grep 按关键字搜索行
B. cut 按指定分隔符剪切字段
C. sort 对文件内容进行排序
D. awk 不能用于处理文本

32. 关于 Linux shell 特殊变量，以下说法正确的有？（多选）
A. $# 表示脚本参数的个数
B. $? 表示上一条命令的返回值
C. $* 表示所有参数
D. $0 表示脚本的最后一个参数

33. 以下属于 HTTP 常见请求方法的有？（多选）
A. GET
B. DELETE
C. PATCH
D. OPTIONS

34. 关于 Selenium 隐式等待与显式等待，以下说法正确的有？（多选）
A. 隐式等待是全局设置，作用于所有元素查找
B. 显式等待通过 WebDriverWait + until 实现
C. 显式等待针对特定元素或特定条件
D. 设置隐式等待后，元素不存在会立即报错

35. 关于 TCP 四次挥手，以下说法正确的有？（多选）
A. 用于释放 TCP 连接
B. 第一次挥手由主动关闭方发送 FIN
C. 整个断开过程共需要 4 个报文段
D. 四次挥手与三次握手的报文类型完全相同

---

## 三、填空题（共 10 题）

36. Python 中，单行注释以 ______ 开头。

37. Python 中，表示"空值 / 无值"的关键字是 ______。

38. Python re 模块中，用于替换字符串中匹配项的函数是 re.______。

39. 正则匹配对象的方法中，groups() 返回包含所有小组字符串的 ______（填写数据类型）。

40. MySQL 合并查询中，union ______ 表示不去重地合并两个查询结果。

41. MySQL 中，删除外键约束的命令格式为 alter table 从表名 drop ______ 外键名。

42. Linux shell 特殊变量中，用于获取脚本名称的变量是 ______。

43. Linux 中，用于按文件名查找文件或目录的命令是 ______。

44. Git 中，用于设置全局用户信息（用户名 / 邮箱）的命令是 git ______。

45. Selenium 中，设置全局隐式等待、超时后抛出异常的方法是 driver.______(10)。

---

## 四、简答题（共 5 题）

46. 请简述 Python 中 re.match 与 re.search 的区别，并说明匹配对象的 group() 与 groups() 方法各自返回什么。

47. 请简述 MySQL 中主键约束与外键约束的区别，并说明"多对多"关系在数据库中通常如何实现（需要几张表、外键如何设置）。

48. 请简述 TCP 四次挥手释放连接的过程（四次挥手各次发送什么报文段），并说明为什么断开连接需要四次而不是三次。

49. 请简述 Linux shell 特殊变量 $#、$?、$*、$@、$0 各自的含义。

50. 请简述 Selenium 中隐式等待（implicitly_wait）与显式等待（WebDriverWait + expected_conditions）的区别，并说明各自适用的场景。
