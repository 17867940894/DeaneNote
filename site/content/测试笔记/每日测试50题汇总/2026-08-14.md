# 每日测试50题纯练习卷

> 日期：2026-08-14
> 总题量：50道 | 单选25 / 多选10 / 填空10 / 简答5
> 错题混入：0道（错题集当前为空）
> 纯练习卷，无答案无解析

---

## 一、单选题（共25题）

### 1. 软件测试的 3 个目的不包括以下哪项？
    A. 发现缺陷
    B. 验证需求
    C. 建立信心
    D. 提高开发效率

### 2. 在常见服务端口号中，MySQL 数据库服务默认使用的端口是？
    A. 21（FTP）
    B. 22（SSH）
    C. 8080（代理/调试）
    D. 3306（MySQL）

### 3. 在测试执行流程中，"冒烟测试"的主要目的是？
    A. 验证核心功能是否可用，决定是否继续后续测试
    B. 覆盖所有代码分支的测试
    C. 测试长时间运行的稳定性
    D. 验证系统在不同浏览器上的兼容性

### 4. 单元测试、集成测试、系统测试中，集成测试的测试对象是？
    A. 最小可测单元（函数、类、模块）
    B. 完整的软件系统
    C. 用户需求文档
    D. 模块之间的接口和交互

### 5. 关于 Alpha 测试与 Beta 测试，以下描述正确的是？
    A. 两者完全相同，没有区别
    B. 执行地点不同：Alpha 在开发环境（内部），Beta 在用户真实环境（外部）
    C. 两者都由外部最终用户执行
    D. 两者都在产品发布之后进行

### 6. 在 OSI 参考模型中，负责"建立、管理和终止通信会话"的是哪一层？
    A. 传输层
    B. 网络层
    C. 表示层
    D. 会话层

### 7. 在 OSI 参考模型中，物理层的主要功能是？
    A. 负责路径选择与逻辑寻址
    B. 实现相邻节点之间比特流的透明传输
    C. 负责数据编码、格式转换与加密
    D. 负责建立和管理通信会话

### 8. IPv4 地址的二进制长度是？
    A. 16 位
    B. 32 位
    C. 64 位
    D. 128 位

### 9. 在 IP 地址中，127.0.0.1 表示的是？
    A. 组播地址
    B. 有限广播地址
    C. 本机回环地址
    D. 公网可用地址

### 10. HTTP/3 相比 HTTP/1.1 和 HTTP/2，其底层传输协议改为？
    A. UDP + QUIC
    B. TCP
    C. IP
    D. FTP

### 11. HTTP 状态码 401 Unauthorized 表示？
    A. 未认证，需提供有效凭证
    B. 请求成功，返回期望的响应
    C. 请求的资源不存在
    D. 服务器内部错误

### 12. HTTP 状态码 403 Forbidden 表示？
    A. 请求语法错误或参数无效
    B. 请求的资源不存在
    C. 认证通过但无权限访问该资源
    D. 服务器过载或维护中

### 13. 在 Linux 中，执行 `chmod 755` 后，文件权限对应的含义是？
    A. 属主、属组、其他用户均无任何权限
    B. 属主 rwx，属组 rwx，其他用户 rwx
    C. 仅属主可读写，属组和其他用户无权限
    D. 属主 rwx（读写执行），属组 r-x，其他用户 r-x

### 14. 在 Linux 中，用于创建软链接（符号链接）的命令是？
    A. ln -s
    B. cp
    C. mv
    D. rm

### 15. Linux 中，与 `ps`（一次性进程快照）相比，`top` 命令的特点是？
    A. 只能查看一个进程
    B. 实时动态刷新，展示进程与系统资源占用
    C. 用于编辑文本文件
    D. 用于查看磁盘空间

### 16. 在 Selenium 中，用于清空输入框已有内容的元素操作方法是？
    A. click()
    B. send_keys()
    C. clear()
    D. refresh()

### 17. 在 Selenium 中，`get_attribute()` 方法的作用是？
    A. 获取元素的可见文本
    B. 获取元素在页面中的坐标
    C. 获取元素的尺寸大小
    D. 获取元素的 HTML 属性值

### 18. 在 Selenium 中，关于 close() 与 quit() 方法的区别，正确的是？
    A. close() 只关闭当前窗口，驱动进程仍在运行
    B. quit() 只关闭当前窗口，驱动进程仍在运行
    C. close() 关闭所有窗口并退出驱动进程
    D. 两者作用完全相同，没有区别

### 19. 在 JMeter 中，下列取样器中"不发送实际请求、用于调试查看变量值"的是？
    A. HTTP 请求
    B. JDBC 请求
    C. 调试取样器
    D. 响应断言

### 20. 在 JMeter 线程组中，启用"调度器"设置持续运行时长时，循环次数应如何设置？
    A. 必须设置为 0
    B. 需勾选"永远"
    C. 必须设置为 1
    D. 无需进行任何设置

### 21. 在 Git 中，用于将远程仓库完整克隆到本地（含历史记录）的命令是？
    A. git clone
    B. git commit
    C. git branch
    D. git log

### 22. 在 MySQL 中，用于删除整张表（含表结构与数据）的命令是？
    A. create table
    B. alter table
    C. drop table
    D. select table

### 23. Python 中，字符串 `"123456789"` 执行切片 `"123456789"[0:-1]` 后的结果是？
    A. "123456789"
    B. "23456789"
    C. "12345678"
    D. "987654321"

### 24. 关于 JDK、JRE、JVM 三者的包含关系，正确的是？
    A. JVM 中包含 JDK
    B. JDK 包含 JRE，JRE 包含 JVM
    C. 三者相互独立，互不包含
    D. JRE 中包含 JDK

### 25. ES6 中，使用 `let` 声明的变量其作用域是？
    A. 全局作用域
    B. 函数作用域
    C. 块级作用域（所在的代码块内有效）
    D. 无作用域限制

---

## 二、多选题（共10题）

### 26. 关于软件测试的分类，以下描述正确的有？（多选）
    A. 按是否执行代码分为静态测试和动态测试
    B. 按是否关注内部结构分为黑盒、白盒、灰盒测试
    C. 按测试阶段分为单元、集成、系统、验收测试
    D. 按执行方式只有手工测试一种

### 27. 关于 OSI 参考模型各层的功能，以下对应正确的有？（多选）
    A. 应用层：为应用程序提供 HTTP 等服务
    B. 传输层：提供 TCP/UDP 端到端传输服务
    C. 网络层：负责 IP 寻址与路由选择
    D. 物理层：负责数据加密与格式转换

### 28. 关于 TCP/IP 四层模型，以下对应正确的有？（多选）
    A. 应用层：HTTP、DNS、FTP 等协议
    B. 传输层：TCP 与 UDP 协议
    C. 网络层：IP 协议负责寻址与路由
    D. 网络接口层：负责数据加密与压缩

### 29. 在 Linux 中，以下哪些属于用户管理相关的命令？（多选）
    A. useradd
    B. passwd
    C. userdel
    D. find

### 30. 关于 Selenium 的浏览器操作，以下描述正确的有？（多选）
    A. driver.get(url) 用于打开指定网页
    B. driver.maximize_window() 用于最大化浏览器窗口
    C. driver.back() 用于后退到上一页
    D. driver.delete() 用于删除浏览器缓存

### 31. 关于 JMeter 线程组的属性，以下描述正确的有？（多选）
    A. 线程数表示模拟的虚拟用户数（并发量）
    B. 循环次数默认为 1 次
    C. 调度器的"持续时间"表示线程组连续运行的时长
    D. 调度器的"启动延迟"表示线程组启动前的固定准备时间

### 32. 关于 Python 的数据类型，以下描述正确的有？（多选）
    A. list（列表）元素可变，支持增删改操作
    B. tuple（元组）创建后不可修改
    C. dict（字典）以键值对形式存储数据
    D. int 类型用于表示浮点数

### 33. 关于 MySQL 的登录与退出方式，以下描述正确的有？（多选）
    A. 使用 mysql -u用户名 -p密码 方式登录
    B. 使用 mysql -h主机名 -P端口号 -u用户名 -p密码 可指定主机与端口登录
    C. 输入 exit 或 quit 可以退出 MySQL
    D. 按 ctrl+z 可以退出 MySQL

### 34. 关于 Python 语言的特点，以下描述正确的有？（多选）
    A. 使用缩进表示代码块，无需大括号
    B. 单行注释以 # 开头
    C. 字符串可以用 + 连接、用 * 重复
    D. Python 有单独的字符类型 char

### 35. 关于 Java 语言，以下描述正确的有？（多选）
    A. 具有跨平台特性，一次编写、到处运行
    B. Java 程序的入口方法是 main()
    C. 通过 System.out.println() 在控制台输出内容
    D. Java 语言由微软公司开发

---

## 三、填空题（共10题）

### 36. 按照 ISO 9000 的定义，质量是一组______特性满足要求的程度。

### 37. HTTP 状态码 502 Bad Gateway 表示______错误。

### 38. 在 MySQL 中，用于查看当前服务器中所有数据库列表的命令是 show ______。

### 39. 在 Selenium 中，用于刷新当前页面的方法是 driver.______()。

### 40. 在 MySQL 中，用于查看表结构的命令是______（填写命令格式，如"命令 表名"）。

### 41. Python 中，字符串 "abc" * 3 的运算结果是______。

### 42. Java 中，用于将 .java 源文件编译为 .class 字节码文件的命令是______。

### 43. ES6 中，const 声明的常量在初始化后______（填写"可以"或"不可以"）重新赋值。

### 44. 在 MySQL 中，用于指定使用某个数据库的命令是 ______（填写命令格式，如"命令 库名"）。

### 45. 在 Git 中，用于将本地提交推送到远程仓库的命令是 git ______。

---

## 四、简答题（共5题）

### 46. 请简述软件缺陷管理（从缺陷发现到关闭）的完整流程，并列举常见的缺陷状态。

### 47. 请简述 HTTP 协议"无状态"特性的含义，并说明 Cookie 与 Session 在解决该问题时的作用与区别。

### 48. 请简述 Web 前端开发中 HTML、CSS、JavaScript 三者各自的作用与分工。

### 49. 请简述性能测试中常用的性能指标（并发量、响应时间、吞吐率、成功率）各自的含义。

### 50. 请简述 Java 面向对象的三大特性（封装、继承、多态）各自的含义，并分别举例说明。

*（内容由AI生成，仅供参考）*
