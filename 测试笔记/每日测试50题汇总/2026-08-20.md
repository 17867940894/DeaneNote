# 每日测试 50 题（纯练习卷）

> 日期：2026-08-20（周四）　|　题型配比：单选 25 / 多选 10 / 填空 10 / 简答 5
> 本卷为**纯练习卷**，仅含题干，不含答案、解析与评分要点，便于直接复制自测。

---

## 一、单项选择题（1–25，每题只有一个正确选项）

1. 在 Selenium 处理极验类滑块验证码时，通常先用 OpenCV 模板匹配识别出缺口位置，再将缺口像素坐标乘以"页面缩放比例 factor"后拖动。引入 factor 主要是为了解决什么问题？
   A. 截图分辨率与浏览器实际渲染尺寸不一致导致坐标偏移
   B. 滑块元素被其他弹窗遮挡
   C. 验证码图片采用懒加载尚未完全加载
   D. 浏览器与服务器之间网络延迟

2. 模拟真人拖动滑块时，轨迹通常设计为"先加速后减速、末尾轻微过冲并叠加随机抖动"，其主要目的是？
   A. 提升 Selenium 脚本的整体执行速度
   B. 绕过基于"匀速机械轨迹"的自动化风控识别
   C. 减少 ActionChains 的方法调用次数
   D. 避免拖拽坐标超出浏览器窗口边界

3. 网站常通过检测 `window.navigator.webdriver` 是否为 true 来识别自动化。在 Selenium 中借助 CDP 在每次新文档加载前注入脚本去除该特征，应使用下列哪条命令？
   A. driver.delete_all_cookies()
   B. driver.set_window_size(0, 0)
   C. driver.execute_cdp_cmd("Page.addScriptToEvaluateOnNewDocument", {...})
   D. driver.switch_to.alert.dismiss()

4. 在 Python 自动化识别"文字点选类"验证码时，常用于本地轻量 OCR 识别的库是？
   A. opencv-python
   B. Pillow
   C. pytesseract
   D. ddddocr

5. 使用 pytest-xdist 插件实现用例多进程并行执行时，常用的自动分配 worker 数量的命令参数是？
   A. -n auto
   B. -p no:cacheprovider
   C. -x
   D. -q

6. 在 Selenium 并发测试中，往往优先选择"多进程（pytest-xdist）"而非"多线程（ThreadPoolExecutor）"，其主要原因是？
   A. 多线程会显著增加物理内存占用
   B. CPython 的 GIL 使同进程内多线程无法真正并行执行 Python 字节码，多进程可绕过该限制
   C. 多线程会导致 WebDriver 无法启动
   D. 多进程无法在用例之间共享测试数据

7. 在并发自动化测试中，为避免多个并发用例相互干扰（如共享登录态、竞争同一资源），每个并发进程/线程应当？
   A. 所有进程共享同一个 WebDriver 实例
   B. 共用同一个浏览器标签页
   C. 各自创建独立的 WebDriver 实例
   D. 关闭所有等待机制以提升并发速度

8. pytest-xdist 中，若希望某些用例强制在同一 worker 上串行执行（如共享同一账号以避免并发冲突），应使用的标记是？
   A. @pytest.mark.parametrize
   B. @pytest.mark.skip
   C. @pytest.mark.flaky
   D. @pytest.mark.xdist_group(name="g1")

9. 通过 `window.performance.timing` 计算"页面完全加载总耗时"，常用的公式是？
   A. loadEventEnd - navigationStart
   B. domContentLoadedEventEnd - navigationStart
   C. responseEnd - requestStart
   D. fetchStart - navigationStart

10. Google Core Web Vitals 中，用于衡量"最大内容绘制（页面主要内容渲染完成时间点）"的指标是？
    A. FID（首次输入延迟）
    B. CLS（累积布局偏移）
    C. LCP（最大内容绘制）
    D. TTFB（首字节时间）

11. Core Web Vitals 对 LCP（最大内容绘制）的"良好（Good）"阈值要求是？
    A. ≤ 1.0 秒
    B. ≤ 2.5 秒
    C. ≤ 4.0 秒
    D. ≤ 6.0 秒

12. 衡量页面视觉稳定性、反映元素在加载过程中发生意外位移程度的 Core Web Vitals 指标是？
    A. LCP
    B. FID
    C. INP
    D. CLS（累积布局偏移）

13. 在 Selenium 中获取 Chrome 底层性能日志（如 Page 域的导航、加载事件），通常借助？
    A. CDP（Chrome DevTools Protocol）
    B. JMeter 聚合报告
    C. Postman Monitor
    D. Git pre-commit 钩子

14. 想要在页面运行过程中实时监听并采集 Web Vitals 指标的变化，现代前端通常使用的 API 是？
    A. performance.timing 一次性快照
    B. PerformanceObserver API
    C. console.log 手动埋点
    D. localStorage 定时轮询

15. 在 Apifox 后置脚本中，获取当前脚本所处事件名称（如 pre_request / test）的属性是？
    A. pm.request.name
    B. pm.response.code
    C. pm.info.eventName
    D. pm.environment.name

16. 在 Apifox 前置脚本中，需要主动调用另一个接口（如先获取 token 再用于后续请求），应使用的 API 是？
    A. pm.test()
    B. pm.expect()
    C. pm.cookies.get()
    D. pm.sendRequest()

17. 在 Apifox 变量体系中，当多个作用域存在同名变量时，作用域为"局部变量（pm.variables 设置）"的优先级通常？
    A. 最高（当前请求内有效，覆盖其他作用域）
    B. 低于环境变量
    C. 低于全局变量
    D. 低于数据变量

18. 在 Apifox 后置脚本中，将响应体解析为 JSON 对象以便提取字段，应使用？
    A. pm.response.text()
    B. pm.response.json()
    C. pm.request.body
    D. pm.cookies.get()

19. 在 Apifox 后置脚本中断言 HTTP 状态码等于 200，可使用的表达式是？
    A. pm.response.status
    B. pm.response.json().code
    C. pm.response.code === 200
    D. pm.environment.get("code")

20. 在 Apifox 脚本中，读取响应 Set-Cookie 中某个 cookie 的值，应使用？
    A. pm.globals.get()
    B. pm.request.headers
    C. pm.test()
    D. pm.cookies.get("name")

21. 使用 Python mysql-connector 建立与 MySQL 的连接，核心代码是？
    A. mysql.connector.connect(host=..., user=..., password=..., database=...)
    B. mysql.connector.open(...)
    C. mysql.connector.create(...)
    D. mysql.connector.link(...)

22. 在 mysql-connector 中，执行一条 SQL 查询并获取结果集的标准流程是？
    A. conn.run(sql); conn.fetchall()
    B. cur = conn.cursor(); cur.execute(sql); cur.fetchall()
    C. conn.execute(sql).all()
    D. db.query(sql).result()

23. 使用 mysql-connector 批量插入多条记录（相比循环 execute 性能更好），应使用？
    A. cursor.execute 循环调用
    B. conn.batch()
    C. cursor.executemany(sql, data_list)
    D. cursor.bulk_insert()

24. 在 mysql-connector 中，防止 SQL 注入的正确做法是？
    A. 使用字符串拼接组装 SQL
    B. 执行前先关闭数据库连接
    C. 使用 eval 执行 SQL 文本
    D. 使用 %s 占位符并传入参数元组

25. 在 mysql-connector 执行 INSERT/UPDATE/DELETE 之后，要使修改真正落库，必须调用？
    A. conn.commit()
    B. cursor.close()
    C. conn.flush()
    D. cursor.commit()

---

## 二、多项选择题（26–35，每题至少有两个正确选项）

26. 以下哪些属于 SVN（Subversion）的常用命令？
    A. svn checkout（检出工作副本）
    B. svn update（更新到最新版本）
    C. svn commit（提交本地修改）
    D. svn revert（撤销本地未提交修改）

27. 在 Android 系统中，以下属于"四大组件（Component）"的有？
    A. Activity（活动）
    B. Service（服务）
    C. BroadcastReceiver（广播接收器）
    D. ContentProvider（内容提供器）

28. JMeter 中，以下哪些属于"定时器（Timer）"元件？
    A. 固定定时器（Constant Timer）
    B. 高斯随机定时器（Gaussian Random Timer）
    C. 同步定时器（Synchronizing Timer，集合点）
    D. 吞吐量定时器（Constant Throughput Timer）

29. 在 Postman / Apifox 中，变量作用域通常包含以下哪些层级（由低到高或按可见范围）？
    A. 局部变量（Local / 请求内）
    B. 数据变量（Data，由外部数据文件驱动）
    C. 环境变量（Environment）
    D. 全局变量（Global）

30. 关于使用 Newman 在命令行执行 Postman 集合，以下说法正确的有？
    A. 基本命令形如 `newman run 集合文件.json`
    B. 可通过 `-e` 指定环境变量文件
    C. 可通过 `-r html,json` 指定生成的报告类型
    D. 可直接读取 Apifox 导出的 .har 文件作为集合

31. 在 Selenium 并发（多进程/多线程）测试中，常见问题或需要注意的事项包括？
    A. 多个 driver 实例争用同一份测试数据文件导致冲突
    B. 并发下元素定位不稳定、偶发超时或失效
    C. 共享全局状态（如同一账号登录态）引发用例相互影响
    D. 应确保每个并发单元拥有独立的 WebDriver 实例与隔离数据

32. 以下哪些属于常见的前端页面性能监控与采集手段？
    A. Performance API（performance.timing / getEntries）
    B. CDP（Chrome DevTools Protocol）性能日志
    C. Lighthouse 自动化审计
    D. PerformanceObserver 实时监听

33. 使用 JMeter 以非 GUI（命令行）模式运行脚本并生成 HTML 报告，涉及到的参数包括？
    A. -n（非 GUI 模式）
    B. -t（指定测试脚本 .jmx）
    C. -l（指定结果日志文件）
    D. -e -o（生成 HTML 报告及输出目录）

34. 在 Python mysql-connector 中，以下哪些属于 cursor 对象的方法/属性？
    A. execute(sql)
    B. executemany(sql, seq)
    C. fetchall() / fetchone()
    D. rowcount（受影响行数）

35. 关于 SVN 与 Git 的区别，以下说法正确的有？
    A. SVN 是集中式版本控制，Git 是分布式版本控制
    B. SVN 的版本号是全局递增的整数，Git 使用 SHA-1 哈希标识提交
    C. SVN 默认必须连接中央服务器才能提交，Git 支持离线提交
    D. SVN 工作副本中含 .svn 目录记录元数据，Git 在工作区根目录有 .git 目录

---

## 三、填空题（36–45，请补全横线处内容）

36. 在 Selenium 中，使用 CDP 去除自动化特征时，常用命令是 `driver.execute_cdp_cmd("Page.________________", {...})`，该方法会在每个新文档加载前注入脚本。

37. pytest-xdist 实现多进程并行的命令参数格式为 `pytest -n ______`（填写自动分配 worker 数量的关键字）。

38. 在 `window.performance.timing` 中，从导航开始到页面 load 事件结束的总耗时可表示为 `______ - navigationStart`。

39. Google Core Web Vitals 中，FID（首次输入延迟）的"良好"阈值是小于 ______ 毫秒。

40. 在 Apifox 脚本中，主动发起一个 HTTP 请求（常用于前置脚本中获取 token）的方法是 `pm.______()`。

41. 使用 Python mysql-connector 时，获取查询结果所有行的方法是 `cursor.______()`。

42. 在 mysql-connector 中，执行批量插入应使用 `cursor.______(sql, data_list)` 方法。

43. SVN 中，撤销本地未提交的修改、恢复到版本库状态的命令是 `svn ______`。

44. SVN 中，从版本库首次获取工作副本的命令是 `svn ______`。

45. 在 Postman 生态中，用于从命令行批量运行集合并集成到 CI 的官方工具是 ______。

---

## 四、简答题（46–50，请简要作答）

46. 请简述在 Selenium 中处理滑块验证码（如极验）的基本思路，至少包括：如何识别缺口位置、如何根据截图缩放比例 factor 计算实际拖动坐标、如何构造拟真拖动轨迹（加速→减速→过冲→抖动）。

47. 请简述 pytest-xdist 多进程并行与 ThreadPoolExecutor 多线程并发在执行 Selenium 用例时的主要区别，并说明为何自动化测试中常优先选择多进程方式。

48. 请简述 Google Core Web Vitals 三大指标（LCP、FID/INP、CLS）各自衡量的内容，以及各自"良好（Good）"的阈值标准。

49. 请简述使用 Python mysql-connector 操作 MySQL 的标准流程（连接 → 获取游标 → 执行 → 获取结果 → 提交/回滚 → 关闭），并说明使用 `%s` 占位符防止 SQL 注入的原理。

50. 请简述 SVN 与 Git 在"版本号机制"和"离线工作能力"上的主要区别，并说明在 SVN 中发生冲突后常见的解决步骤（含冲突标记的含义）。
