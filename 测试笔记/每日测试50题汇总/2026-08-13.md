---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: a09a35d287af0e0724f69262dadf19a9_2c4d6e8f0a1b2c3d4e5f60718293a4b5c
    ReservedCode1: 1tJL5/0LoLdKcKHtsXXPf3lmWJLMkOIENP0/wHTjoaOJfZUWXDGWIyGQ4vTUAfMixiOjsI0APnX7G3QQgetkR2gevAis+Ee7hS0ZD3Na/E3meyJzHP/CYE63+af+niZHJ7SEOITtM7NL+t3egaGk6z8qQn6DY6f86Bw9NGhLyJQHlrwER02WCc29Evc=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: a09a35d287af0e0724f69262dadf19a9_2c4d6e8f0a1b2c3d4e5f60718293a4b5c
    ReservedCode2: 1tJL5/0LoLdKcKHtsXXPf3lmWJLMkOIENP0/wHTjoaOJfZUWXDGWIyGQ4vTUAfMixiOjsI0APnX7G3QQgetkR2gevAis+Ee7hS0ZD3Na/E3meyJzHP/CYE63+af+niZHJ7SEOITtM7NL+t3egaGk6z8qQn6DY6f86Bw9NGhLyJQHlrwER02WCc29Evc=
---

# 每日测试50题纯练习卷

> 日期：2026-08-13
> 总题量：50道 | 单选25 / 多选10 / 填空10 / 简答5
> 错题混入：0道（错题集当前为空）
> 纯练习卷，无答案无解析

---

## 一、单选题（共25题）

### 1. 微信、QQ 这类需要安装独立客户端、聊天数据存储在云端服务器、通过私有协议与服务器通信的软件，属于哪种网络架构？
    A. C/S（客户端/服务器）
    B. B/S（浏览器/服务器）
    C. P2P（点对点）
    D. 混合架构

### 2. 迅雷、BT 下载时每个节点既从其他用户获取数据片段、又把自己已下载的部分上传给别人，这种传输方式属于？
    A. C/S（客户端/服务器）
    B. P2P（点对点）
    C. B/S（浏览器/服务器）
    D. 集中式架构

### 3. 在性能测试领域，常说的"性能测试"通常专指？
    A. 前端性能测试
    B. 单元性能测试
    C. 接口性能测试
    D. 后端性能测试

### 4. JMeter 5.6 运行所依赖的运行环境是？
    A. Java 8 或更高版本（JDK 8/11/17 均可）
    B. C 语言运行时
    C. Python 解释器
    D. 无需任何运行环境

### 5. JMeter 进行分布式测试时，控制机和执行机都需要在 `jmeter.properties` 中修改哪一项配置来关闭 RMI 的 SSL？
    A. language=zh_CN
    B. remote.ssl=false
    C. ssl.disable=true
    D. server.rmi.ssl.disable=true

### 6. 在 JMeter 中，需要从响应 body 的 JSON 数据中提取某个节点值（如 `$.data.session.sid`）保存为变量，应使用哪种后置处理器？
    A. 正则表达式提取器
    B. JSON 提取器
    C. XPath2 提取器
    D. CSS 提取器

### 7. 在 JMeter 中，需要从响应 body 的 HTML 文字中提取信息保存为变量，应使用哪种提取器？
    A. 正则表达式提取器
    B. JSON 提取器
    C. XPath2 提取器
    D. JSR223 提取器

### 8. JMeter JSON 提取器中，Match No.（匹配数字）设置为 -1 表示？
    A. 随机保存一个匹配值
    B. 只保存第 1 个匹配值
    C. 只保存第 n 个匹配值
    D. 保存所有匹配值

### 9. JMeter JSON 提取器勾选"Compute concatenation var"（联合保存）后，若原变量名为 uid，则生成的联合变量名称是？
    A. uid_1
    B. uid
    C. uid_ALL
    D. uid_total

### 10. 关于 JMeter 元件的作用域，以下描述正确的是？
    A. 逻辑控制器可以管理线程组下所有层级的取样器
    B. 逻辑控制器只能管理其下的取样器
    C. 逻辑控制器只能管理与其同级的取样器
    D. 逻辑控制器可以跨线程组管理取样器

### 11. 在 JMeter 中，用于查看每个请求的响应信息、排查脚本错误的监听器是？
    A. 聚合报告
    B. 图形结果
    C. 查看结果树
    D. 断言结果

### 12. 根据性能测试经验，内网系统的并发量通常范围是？
    A. 10~100
    B. 1000~5000
    C. 10000 以上
    D. 没有参考范围

### 13. Git 是一种什么类型的版本控制系统？
    A. 集中式版本控制系统
    B. 分布式版本控制系统
    C. 本地式版本控制系统
    D. 共享式版本控制系统

### 14. 在 Git 中，执行 `git init` 命令的作用是？
    A. 创建版本库（生成 .git 目录）
    B. 查看版本库状态
    C. 提交代码到远程仓库
    D. 删除版本库

### 15. 在 Git 中，`git add a.txt` 命令的作用是？
    A. 提交到版本库
    B. 查看文件状态
    C. 将工作空间的修改添加到暂存区
    D. 删除文件

### 16. 在 Git 中，`git checkout 版本号` 命令的作用是？
    A. 提交当前修改
    B. 将历史版本同步到工作空间
    C. 创建新分支
    D. 合并分支

### 17. SVN（Subversion）属于哪种版本控制系统？
    A. 集中式版本控制系统
    B. 分布式版本控制系统
    C. 混合式版本控制系统
    D. 点对点版本控制系统

### 18. 在常见的数据库管理系统中，适合作为移动端嵌入式小型数据库的是？
    A. MySQL
    B. Oracle
    C. SQLite
    D. SQL Server

### 19. 在 MySQL 中，数据库的"表/记录/字段"分别对应 Java 代码中的？
    A. 包/类/方法
    B. 类/对象/属性
    C. 文件/行/列
    D. 表/行/列

### 20. 在 Postman 中，编写断言脚本（基于 Node.js 实现请求验证）所使用的核心对象是？
    A. request 对象
    B. response 对象
    C. pm 对象
    D. console 对象

### 21. 在 Postman 中，一组相关请求的容器（相当于测试套件，可导出 JSON 交给 Newman 执行）称为？
    A. 环境（Environment）
    B. 工作区（Workspace）
    C. 集合（Collection）
    D. 监视器（Monitor）

### 22. Selenium 中，`ActionChains(wd).click_and_hold(source).move_by_offset(200, 0).release().perform()` 这段代码实现的典型操作是？
    A. 双击元素
    B. 右键单击元素
    C. 鼠标悬停元素
    D. 按住元素拖动后释放（模拟拖拽，常用于滑块）

### 23. Selenium 中，需要获取 `display:none` 隐藏元素的文本（原生 .text 返回空），通过 `execute_script` 应返回元素的哪个属性？
    A. innerText
    B. textContent
    C. value
    D. outerHTML

### 24. Selenium 中，处理原生 Alert 弹窗时，用于显式等待弹窗出现的 expected_conditions 条件是？
    A. alert_is_displayed
    B. alert_present
    C. wait_alert
    D. alert_is_present

### 25. 采用 PO（Page Object）模式后，页面 UI 发生变化时，通常只需要修改？
    A. 所有测试用例
    B. 断言代码
    C. 测试数据文件
    D. 对应的页面对象类

---

## 二、多选题（共10题）

### 26. Selenium 中，以下哪些属于 ActionChains 提供的鼠标操作方法？（多选）
    A. move_to_element()（鼠标悬停）
    B. context_click()（右键单击）
    C. double_click()（双击）
    D. implicitly_wait()（隐式等待）

### 27. 关于 Selenium 中执行 JavaScript（execute_script）的适用场景，以下描述正确的有？（多选）
    A. 元素在视口外时通过 scrollIntoView() 滚动到可见
    B. 获取 display:none 隐藏元素的文本
    C. 富文本编辑器（contenteditable div）中设置内容
    D. 普通文本框输入应优先用 execute_script 代替 send_keys

### 28. 关于 Selenium 中的原生弹窗，以下说法正确的有？（多选）
    A. alert 弹窗只有确定按钮
    B. confirm 弹窗有确定和取消按钮
    C. prompt 弹窗可以在 accept 之前输入文本
    D. 同一弹窗 accept() 之后还可以再次 accept()

### 29. 在 JMeter 中，以下哪些属于后置处理器？（多选）
    A. JSON 提取器
    B. 正则表达式提取器
    C. CSS 提取器
    D. 响应断言

### 30. 在 JMeter 中，以下哪些属于常用的断言元件？（多选）
    A. JSON 断言
    B. 响应断言
    C. 大小断言
    D. 正则表达式提取器

### 31. 在 Git 中，以下哪些属于常用命令？（多选）
    A. git add
    B. git commit
    C. git log
    D. git compile

### 32. 以下哪些属于常用的接口测试工具？（多选）
    A. Postman
    B. Apifox
    C. JMeter
    D. curl

### 33. 以下哪些属于关系型数据库管理系统？（多选）
    A. MySQL
    B. Oracle
    C. SQL Server
    D. MongoDB

### 34. 在 pytest + Selenium 框架中，数据驱动测试支持从以下哪些外部文件加载测试数据？（多选）
    A. CSV
    B. Excel
    C. YAML
    D. JSON

### 35. 关于 ADB 的 `input keyevent` 命令，以下对应正确的有？（多选）
    A. keyevent 3 返回桌面（Home）
    B. keyevent 4 返回上一页
    C. keyevent 26 电源键（亮屏/锁屏）
    D. keyevent 100 音量增大

---

## 三、填空题（共10题）

### 36. Selenium 中，ActionChains 的方法只是"录制"动作，必须调用 ______ 方法后动作才会真正执行到浏览器上。

### 37. Selenium 的 `execute_script` 中，Python 传入的参数在 JS 脚本里通过 ______ 接收（如第 1 个参数）。

### 38. Selenium 中，对 confirm 弹窗点击"取消"按钮的方法是 alert.______()。

### 39. JMeter 中，下游请求引用上游提取器保存变量的统一语法是 ______（填写变量引用形式）。

### 40. Git 中，用于查看版本库状态（哪些文件被修改/暂存）的命令是 git ______。

### 41. Git 中，`git log --oneline` 中的参数 ______ 用于让每个版本信息只显示一行。

### 42. Postman 中，编写断言脚本（如 pm.test）基于的是名为 ______ 的核心对象。

### 43. 在 adb shell 中执行 su 命令获取 root 权限后，命令行提示符会由 $ 变为 ______。

### 44. 数据库管理系统（Database Management System）的英文缩写是 ______。

### 45. 在 Python 中，处理正则表达式的模块是 ______。

---

## 四、简答题（共5题）

### 46. 请简述 Selenium 中使用 ActionChains 实现"鼠标悬停父菜单 → 移动到子菜单 → 点击子菜单"的操作步骤，并说明为什么必须调用 perform() 方法。

### 47. 请简述 Selenium 处理原生 JS 弹窗（alert / confirm / prompt）的通用步骤，并说明 prompt 弹窗如何在接受前输入文本。

### 48. 请简述分布式版本控制系统（Git）与集中式版本控制系统（SVN）在核心架构上的主要区别，并说明 Git 支持离线提交的原因。

### 49. 请简述 JMeter 中"元件作用域"的含义，并说明逻辑控制器与前置/后置处理器在作用范围上的区别。

### 50. 请简述 Postman 中断言（Tests）的基本写法（pm 对象的常用方法），并说明集合（Collection）与环境（Environment）各自的用途。

*（内容由AI生成，仅供参考）*
