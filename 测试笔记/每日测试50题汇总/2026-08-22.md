# 每日测试卷（2026-08-22）

> 前置校验：四类题型（单选 25 / 多选 10 / 填空 10 / 简答 5）题量充足，无缺题。
> 错题集当前为空（0 道），按情况 B 处理：本卷全部题目从知识库新出，未原样回放错题。
> 本卷已与近 7 天（08-15 / 08-17 / 08-18 / 08-19 / 08-20）已出题干去重，卷内题目亦完全去重。


## 一、单选题（共 25 题）


1. 白盒测试中，"判定-条件覆盖"要求设计并实现一个测试用例集，使其同时满足以下哪两类覆盖？
A. 判定覆盖与条件覆盖
B. 语句覆盖与分支覆盖
C. 条件覆盖与路径覆盖
D. 语句覆盖与条件覆盖


2. 在软件测试度量中，"缺陷密度（Defect Density）"通常定义为？
A. 缺陷数 / 测试用例数
B. 缺陷数 / 千行代码（KLOC）
C. 已修复缺陷 / 总缺陷
D. 发现缺陷 / 总缺陷


3. 验收测试（Acceptance Testing）通常由谁主导或参与执行？
A. 仅开发人员
B. 仅测试人员
C. 用户或客户代表
D. 运维人员


4. 探索性测试中"基于会话的测试管理（SBTM, Session-Based Test Management）"的核心组织单位是？
A. 测试用例
B. 缺陷报告
C. 需求条目
D. 时间盒化的测试会话（Session）


5. 在测试替身（Test Double）分类中，用于验证"某个方法是否被调用、以及调用时的参数"的对象是？
A. Mock
B. Stub
C. Fake
D. Dummy


6. 持续测试（Continuous Testing）在 DevOps 交付流水线中的定位是？
A. 仅在上线后执行
B. 贯穿 CI/CD 各阶段、随每次提交自动执行
C. 仅在需求评审阶段
D. 仅由手工测试完成


7. 消费者驱动的契约测试（Consumer-Driven Contract Testing）常用实现工具是？
A. JUnit
B. JMeter
C. Pact
D. Selenium


8. 模糊测试（Fuzz Testing）主要用于发现哪类问题？
A. 界面布局错位
B. 需求遗漏
C. 性能瓶颈
D. 程序对异常/畸形输入的崩溃或处理异常


9. 在安全测试中，攻击者诱导已登录用户在不知情的情况下提交非预期请求的攻击方式是？
A. CSRF（跨站请求伪造）
B. SQL 注入
C. XSS（跨站脚本）
D. SSRF（服务端请求伪造）


10. 性能测试中"思考时间（Think Time）"模拟的是？
A. 服务器处理请求的时间
B. 用户两次操作之间的停顿间隔
C. 网络传输耗时
D. 数据库查询耗时


11. LoadRunner 中负责脚本录制与开发的组件是？
A. Controller
B. Analysis
C. VuGen
D. Load Generator


12. 性能测试"拐点法"主要用于确定？
A. 最低可运行并发数
B. 平均响应时间
C. 错误率阈值
D. 系统性能出现明显下降的并发拐点（最大承载能力）


13. JMeter 中通过正则表达式从响应中提取动态值的后置处理器是？
A. 正则表达式提取器
B. JSON 提取器
C. XPath 提取器
D. 边界提取器


14. Selenium 4 引入的"相对定位"特性（如 above、toLeftOf、near）称为？
A. CDP
B. Relative Locators
C. BiDi
D. Selenium Manager


15. 在 Page Object 模式中使用 @FindBy 配合 PageFactory.initElements 完成元素懒加载初始化的方式属于？
A. 普通 POM
B. 关键字驱动
C. Page Factory 模式
D. 数据驱动


16. Appium 服务端与被测端驱动（如 UiAutomator2 Server）之间通信所依赖的基础协议是？
A. gRPC
B. MQTT
C. HTTP/2
D. WebDriver 协议（JSON Wire Protocol）


17. Android 测试中用于实时查看设备运行日志、过滤崩溃信息的命令是？
A. adb logcat
B. adb install
C. adb shell
D. adb bugreport


18. 在 ADB 命令中，用于将本地电脑文件推送到 Android 设备指定路径的命令是？
A. adb pull
B. adb push
C. adb install
D. adb shell


19. RESTful 设计中，客户端成功删除资源、服务端无返回内容时常返回的状态码是？
A. 200 OK
B. 201 Created
C. 204 No Content
D. 301 Moved Permanently


20. 与传统的服务器端 Session 认证相比，JWT（JSON Web Token）的主要优势在于？
A. 令牌不可过期
B. 无需签名
C. 体积更小
D. 服务端无需保存会话状态（无状态、易水平扩展）


21. MySQL 四个事务隔离级别中，能完全避免脏读、不可重复读和幻读的是？
A. 串行化（Serializable）
B. 可重复读（Repeatable Read）
C. 读已提交（Read Committed）
D. 读未提交（Read Uncommitted）


22. Redis 缓存问题中，"缓存穿透"指的是？
A. 大量请求集中访问同一个热点 key
B. 大量请求查询根本不存在的数据、绕过缓存直击数据库
C. 大量 key 在同一时刻同时过期
D. Redis 内存被写满


23. Docker 运行容器时，将容器端口映射到宿主机端口使用的参数是？
A. -v
B. -e
C. -p
D. --name


24. Kubernetes 中作为最小部署与调度单元、可包含一个或多个容器的是？
A. Node
B. Service
C. Namespace
D. Pod


25. Jenkins 中用于以代码形式定义构建、测试、部署流水线的文件是？
A. Jenkinsfile
B. pom.xml
C. Makefile
D. docker-compose.yml


---


## 二、多选题（共 10 题）


26. 在典型 CI/CD 流水线中，以下哪些阶段通常属于软件自动交付流程？（多选）
A. 代码构建（Build）
B. 自动化测试（Test）
C. 部署发布（Deploy）
D. 需求评审（属于上游活动，不属流水线执行阶段）


27. 在基于风险的测试（Risk-based Testing）中，应优先安排测试资源的模块通常具有哪些特征？（多选）
A. 失效可能性高
B. 失效影响大
C. 业务优先级高
D. 代码行数最多


28. 以下哪些属于常见的测试替身（Test Double）？（多选）
A. Stub（桩）
B. Mock（模拟）
C. Fake（伪实现）
D. 测试用例


29. 在自动化测试框架设计中，以下哪些是常见的驱动模式？（多选）
A. 数据驱动
B. 关键字驱动
C. 行为驱动（BDD）
D. 仅支持手工驱动


30. JMeter 中，以下哪些属于逻辑控制器（Logic Controller）？（多选）
A. If 控制器
B. While 控制器
C. ForEach 控制器
D. 事务控制器


31. 在 Appium 移动端自动化中，常用的元素定位方式包括以下哪些？（多选）
A. accessibility id
B. UiSelector（UiAutomator）
C. id（resource-id）
D. XPath


32. Python 测试生态中，以下哪些属于常用测试或打桩工具？（多选）
A. pytest
B. unittest
C. unittest.mock（mock / patch）
D. coverage.py


33. 在微服务架构下，为保障接口质量常用以下哪些测试策略？（多选）
A. 契约测试
B. 服务虚拟化
C. 消费者驱动的契约验证
D. 仅做端到端 UI 测试即可


34. 关于 Docker 镜像与容器，以下说法正确的有？（多选）
A. 镜像（Image）是只读模板
B. 容器（Container）是镜像的运行实例
C. 可通过 Dockerfile 构建镜像
D. 容器删除后其数据卷（volume）默认一定被删除


35. 关于 Redis 缓存相关问题与机制，以下说法正确的有？（多选）
A. 缓存穿透是指大量请求查询根本不存在的数据
B. 缓存击穿是指某个热点 key 过期瞬间引发大量并发回源
C. 缓存雪崩是指大量 key 在同一时刻同时过期
D. Redis 仅支持 RDB 持久化，不支持 AOF


---


## 三、填空题（共 10 题）


36. 在软件测试中，将发现的缺陷按"发生可能性 × 影响程度"评估并据此安排测试优先级的测试方法称为 ______ 测试。


37. 在敏捷开发中，用 Given-When-Then 结构编写验收标准、由业务/开发/测试三方达成共识的实践称为 ______（测试/开发）。


38. 在 HTTP 协商缓存机制中，服务端使用 ______ 响应头返回资源的唯一标识，供后续请求校验是否变化（配合 304）。


39. 在 MySQL 中，查看一条 SQL 的执行计划、判断是否走索引，应使用的命令关键字是 ______。


40. Redis 支持的持久化机制主要包括 RDB 快照与 ______ 两种。


41. 在 Linux 中，用于查看网络连接、监听端口等信息的现代命令（常作为 netstat 的替代）是 ______。


42. 在 Docker 中，用于查看容器实时日志输出的命令是 docker ______。


43. 在 Kubernetes 中，用于声明式地将期望状态同步到集群资源的命令是 kubectl ______。


44. Selenium 4 中，能够自动下载并管理浏览器驱动（无需手动配置 driver 路径）的组件称为 Selenium ______。


45. 在性能测试中，单位时间内系统成功处理的事务数量指标称为 ______（如 TPS）。


---


## 四、简答题（共 5 题）


46. 请简述"测试右移（Shift-Right）"的含义，并列举在生产环境侧保障质量的常用手段（如监控告警、混沌工程、A/B 测试、用户反馈闭环等）。


47. 请简述"测试替身（Test Double）"中 Dummy、Stub、Mock、Fake、Spy 五种对象的区别，并说明各自典型使用场景。


48. 请简述持续集成（CI）与持续交付（CD）/持续部署的区别，并说明自动化测试在其中的作用与嵌入位置。


49. 请简述 OWASP Top 10 中至少五种常见 Web 安全漏洞（如 SQL 注入、XSS、CSRF 等），并分别说明其防御要点。


50. 请简述可观测性（Observability）三大支柱——日志（Logging）、指标（Metrics）、链路追踪（Tracing）各自关注什么，并举例说明常用开源工具（如 Prometheus、Grafana、ELK、Jaeger）。
