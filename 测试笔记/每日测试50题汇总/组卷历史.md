## 2026-08-10
1. 在软件生命周期瀑布模型中，设计阶段包含概要设计与详细设计，其中详细设计阶段的主要工作是？
2. 以下关于黑盒测试优点的描述，正确的是？
3. 系统测试主要能发现的缺陷类型通常是？
4. 测试的四个基本活动中，“测试计划”阶段的主要工作是？
5. ISO 25010 质量模型中，描述“功能是否完备、正确、恰当”的特性是？
6. ISO 25010 质量模型中，“兼容性”特性主要包含以下哪两个子特性？
7. 测试用例 8 要素中，“预期结果”描述的是？
8. 缺陷严重程度为“轻微”的缺陷，通常指？
9. 缺陷管理流程中，缺陷回归验证不通过时，缺陷状态通常会被置为？
10. 软件测试七大原则中，“测试活动依赖上下文”的含义是？
11. 以下哪项属于验证（Verification）的典型活动？
12. 在 Linux 中，用于切换当前登录用户（如切换到 root 超级用户）的命令是？
13. 基于经验与历史缺陷推测易错点，从而补充设计测试用例的方法称为？
14. 判定表法（决策表法）主要适用于哪种场景？
15. 对于 URL `https://www.example.com:8443/api/user?id=100`，其中“路径”部分是？
16. 以下哪种场景最适合引入自动化测试？
17. 兼容性测试主要验证的内容是？
18. 关于压力测试（Stress Testing），以下描述正确的是？
19. Selenium 中，使用 find_element 定位元素但页面上不存在该元素时，会？
20. 某元素 class=“s_ipt bg”（含两个类名），直接使用 By.CLASS_NAME 定位会报错，正确的做法是？
21. 在 XPath 中，用于定位文本包含“新闻”二字的 <a> 链接的写法是？
22. HTTP 状态码 201 Created 表示？
23. HTTP 状态码 302 Found 表示？
24. 在 Linux 中，用于在文件中搜索包含指定关键字的行并输出结果的命令是？
25. 在 Android 测试中，用于查看 APK 安装包详细信息的命令行工具是？
26. 以下哪些属于 Selenium 的元素定位方式？
27. 关于 TCP 与 UDP 协议的区别，以下描述正确的有？
28. 以下哪些属于 ADB 的常用命令？
29. 以下哪些属于 SQL 语言的分类？
30. 以下哪些属于常见的软件测试模型？
31. 关于 HTTP 状态码，以下对应正确的有？
32. 以下哪些属于 Selenium 的等待方式？
33. 在 Python 中，以下哪些属于字符串（str）的常用方法？
34. 以下哪些属于移动 App 的异常测试场景？
35. 以下哪些属于 JMeter 脚本中的常用元件？
36. HTTP 协议默认使用的端口号是 ______。
37. TCP 协议在建立连接时，需要进行 ______ 次握手。
38. 在 Linux 中，用于显示当前所在工作目录的命令是 ______。
39. 在 Linux 中，用于查看当前系统进程快照的常用命令是 ______。
40. Selenium 中，find_elements 方法在页面上找不到任何匹配元素时，返回 ______。
41. 在使用 adb logcat 查看日志时，用于先清空历史日志、只输出新日志的参数是 ______。
42. 在 SQL 语言中，SELECT 语句属于 ______ 语言（填写 DDL/DML/DQL/DCL 之一）。
43. Python 中，元素唯一且无序、支持数学集合运算（如并集、交集）的数据类型是 ______。
44. HTTPS 协议默认使用的端口号是 ______。
45. 边界值分析法中，针对等价类边界设计的三个典型点是上点、离点和 ______。
46. 请简述 HTTP 与 HTTPS 协议的主要区别（至少三点），并说明 HTTPS 在传输层使用的加密机制。
47. 请简述 Selenium 元素定位的八大方式，并说明生产环境中选择定位方式的基本原则（推荐优先级）。
48. 请简述 JMeter 性能测试脚本中常用元件（线程组、取样器、断言、监听器、后置处理器）的作用。
49. 请简述 Android 中 monkey 稳定性测试的基本命令格式，并说明 -p、--throttle、-s、-vvv 及 COUNT 各参数的含义。
50. 请简述 SQL 语言的四大分类（DDL/DML/DQL/DCL）各自的作用，并分别写出一个典型关键字。

## 2026-08-11
1. 软件缺陷的判定标准中，以下哪一项不属于缺陷？
2. 在软件测试流程中，需求分析（评审）阶段的参与人员通常不包括以下哪类角色？
3. 开发人员修复缺陷并提交新版本后，测试人员为确认该缺陷已被修复且未引入新问题，通常执行哪种测试？
4. 关于判定表法的使用，以下描述正确的是？
5. 边界值分析法中，"开内闭外"优化策略的含义是？
6. 按照国内软件测试的惯例，单元测试的主要执行人员通常是？
7. 与 HTTP/1.0 相比，HTTP/1.1 的核心改进是？
8. HTTP 状态码 503 Service Unavailable 表示？
9. 关于 OSI 参考模型"表示层"的功能，以下描述正确的是？
10. IPv4 地址中，属于 C 类地址范围的是？
11. 视频直播、在线游戏等场景通常使用 UDP 而不是 TCP，主要原因是？
12. 在 Linux 中，用于查看命令或配置文件帮助信息的通用命令是？
13. Linux 中，命令 tail -f 的主要用途是？
14. 在 Linux 中，ls -l 输出的第一列 drwxr-xr-x 中，第一个字符 d 表示？
15. Linux 中，tar -zcvf houma.tar.gz 选项中 z 的含义是？
16. Linux 中，用于强制终止指定进程的命令是？
17. Selenium 中，find_element(By.LINK_TEXT, '新闻') 对链接文本的要求是？
18. Selenium 显式等待中，WebDriverWait(driver, 10).until(条件) 里 locator 参数的写法要求是？
19. 在 Selenium 中处理 iframe 内的元素时，第一步需要执行的操作是？
20. 性能测试中，"90% 响应时间"的含义是？
21. JMeter 线程组中，Ramp-Up 时间（启动时间）表示？
22. Android 中，adb -s 设备号 shell 命令中 -s 参数的作用是？
23. 在 HTTP 请求方法中，GET 与 POST 的主要区别是？
24. 在 HTML 中，用于引入外部 CSS 样式文件的标签是？
25. Python 中，用于返回序列长度（元素个数）的内置函数是？
26. 缺陷报告提交时，以下哪些属于必须填写或描述的信息？（多选）
27. 以下哪些属于软件缺陷产生的常见原因？（多选）
28. 关于 HTTP 请求，以下描述正确的有？（多选）
29. 以下哪些属于 Linux 的压缩和解压缩类命令？（多选）
30. 关于 TCP 三次握手建立连接的过程，以下描述正确的有？（多选）
31. Selenium 显式等待中，以下哪些属于 expected_conditions 提供的常用条件？（多选）
32. JMeter 中，以下哪些属于常用的控制器类型？（多选）
33. Python 中，以下哪些属于字典（dict）的常用操作方法？（多选）
34. 关于 Android 系统，以下描述正确的有？（多选）
35. 以下哪些属于性能测试的常见分类？（多选）
36. 在 Linux 中，将命令输出结果覆盖写入文件（而非追加）使用的重定向符号是 ______。
37. HTTP 协议工作在 OSI 参考模型的 ______ 层。
38. 在 Linux 中，查看已经执行过的历史命令的命令是 ______。
39. Selenium 显式等待中，WebDriverWait 的 until 方法用于等待条件成立，与之对应、等待条件不再成立时继续执行的方法是 ______。
40. 性能测试中，成功率通常要求达到 ______ 以上（填写数值百分比）。
41. 在 VIM 编辑器中，保存文件并退出的命令是 ______。
42. HTTP/2 相比 HTTP/1.1 在连接复用上的核心改进是 ______，可有效解决队头阻塞问题。
43. 在 Linux 中，用于查看文件系统磁盘空间占用情况的命令是 ______。
44. 在 HTML 中，用于引入外部 JavaScript 文件的标签是 <______>。
45. 在 VIM 编辑器中，从编辑模式回到一般模式需要按下 ______ 键。
46. 请简述软件测试的完整流程（从需求分析到测试报告），并说明每个阶段的主要工作。
47. 请简述 GET 与 POST 请求方法的区别（至少三点），并说明各自适用的典型场景。
48. 请简述 HTTP/1.0 与 HTTP/1.1 的主要区别，并说明 HTTP/1.1 默认使用长连接带来的优势。
49. 请简述 Linux 中 crontab 定时任务的基本格式（五个时间字段各自的含义），并举例说明如何设置"每天凌晨 3 点执行某命令"。
50. 请简述 Linux 中 grep 命令的常用参数及其作用（如 -i、-v、-n、-c、-r），并说明管道符 "|" 的作用。


## 2026-08-13
1. 微信、QQ 这类需要安装独立客户端、聊天数据存储在云端服务器、通过私有协议与服务器通信的软件，属于哪种网络架构？
2. 迅雷、BT 下载时每个节点既从其他用户获取数据片段、又把自己已下载的部分上传给别人，这种传输方式属于？
3. 在性能测试领域，常说的"性能测试"通常专指？
4. JMeter 5.6 运行所依赖的运行环境是？
5. JMeter 进行分布式测试时，控制机和执行机都需要在 jmeter.properties 中修改哪一项配置来关闭 RMI 的 SSL？
6. 在 JMeter 中，需要从响应 body 的 JSON 数据中提取某个节点值（如 $.data.session.sid）保存为变量，应使用哪种后置处理器？
7. 在 JMeter 中，需要从响应 body 的 HTML 文字中提取信息保存为变量，应使用哪种提取器？
8. JMeter JSON 提取器中，Match No.（匹配数字）设置为 -1 表示？
9. JMeter JSON 提取器勾选"Compute concatenation var"（联合保存）后，若原变量名为 uid，则生成的联合变量名称是？
10. 关于 JMeter 元件的作用域，以下描述正确的是？
11. 在 JMeter 中，用于查看每个请求的响应信息、排查脚本错误的监听器是？
12. 根据性能测试经验，内网系统的并发量通常范围是？
13. Git 是一种什么类型的版本控制系统？
14. 在 Git 中，执行 git init 命令的作用是？
15. 在 Git 中，git add a.txt 命令的作用是？
16. 在 Git 中，git checkout 版本号 命令的作用是？
17. SVN（Subversion）属于哪种版本控制系统？
18. 在常见的数据库管理系统中，适合作为移动端嵌入式小型数据库的是？
19. 在 MySQL 中，数据库的"表/记录/字段"分别对应 Java 代码中的？
20. 在 Postman 中，编写断言脚本（基于 Node.js 实现请求验证）所使用的核心对象是？
21. 在 Postman 中，一组相关请求的容器（相当于测试套件，可导出 JSON 交给 Newman 执行）称为？
22. Selenium 中，ActionChains(wd).click_and_hold(source).move_by_offset(200, 0).release().perform() 这段代码实现的典型操作是？
23. Selenium 中，需要获取 display:none 隐藏元素的文本（原生 .text 返回空），通过 execute_script 应返回元素的哪个属性？
24. Selenium 中，处理原生 Alert 弹窗时，用于显式等待弹窗出现的 expected_conditions 条件是？
25. 采用 PO（Page Object）模式后，页面 UI 发生变化时，通常只需要修改？
26. Selenium 中，以下哪些属于 ActionChains 提供的鼠标操作方法？（多选）
27. 关于 Selenium 中执行 JavaScript（execute_script）的适用场景，以下描述正确的有？（多选）
28. 关于 Selenium 中的原生弹窗，以下说法正确的有？（多选）
29. 在 JMeter 中，以下哪些属于后置处理器？（多选）
30. 在 JMeter 中，以下哪些属于常用的断言元件？（多选）
31. 在 Git 中，以下哪些属于常用命令？（多选）
32. 以下哪些属于常用的接口测试工具？（多选）
33. 以下哪些属于关系型数据库管理系统？（多选）
34. 在 pytest + Selenium 框架中，数据驱动测试支持从以下哪些外部文件加载测试数据？（多选）
35. 关于 ADB 的 input keyevent 命令，以下对应正确的有？（多选）
36. Selenium 中，ActionChains 的方法只是"录制"动作，必须调用 ______ 方法后动作才会真正执行到浏览器上。
37. Selenium 的 execute_script 中，Python 传入的参数在 JS 脚本里通过 ______ 接收（如第 1 个参数）。
38. Selenium 中，对 confirm 弹窗点击"取消"按钮的方法是 alert.______()。
39. JMeter 中，下游请求引用上游提取器保存变量的统一语法是 ______（填写变量引用形式）。
40. Git 中，用于查看版本库状态（哪些文件被修改/暂存）的命令是 git ______。
41. Git 中，git log --oneline 中的参数 ______ 用于让每个版本信息只显示一行。
42. Postman 中，编写断言脚本（如 pm.test）基于的是名为 ______ 的核心对象。
43. 在 adb shell 中执行 su 命令获取 root 权限后，命令行提示符会由 $ 变为 ______。
44. 数据库管理系统（Database Management System）的英文缩写是 ______。
45. 在 Python 中，处理正则表达式的模块是 ______。
46. 请简述 Selenium 中使用 ActionChains 实现"鼠标悬停父菜单 → 移动到子菜单 → 点击子菜单"的操作步骤，并说明为什么必须调用 perform() 方法。
47. 请简述 Selenium 处理原生 JS 弹窗（alert / confirm / prompt）的通用步骤，并说明 prompt 弹窗如何在接受前输入文本。
48. 请简述分布式版本控制系统（Git）与集中式版本控制系统（SVN）在核心架构上的主要区别，并说明 Git 支持离线提交的原因。
49. 请简述 JMeter 中"元件作用域"的含义，并说明逻辑控制器与前置/后置处理器在作用范围上的区别。
50. 请简述 Postman 中断言（Tests）的基本写法（pm 对象的常用方法），并说明集合（Collection）与环境（Environment）各自的用途。

## 2026-08-14
1. 软件测试的 3 个目的不包括以下哪项？
2. 在常见服务端口号中，MySQL 数据库服务默认使用的端口是？
3. 在测试执行流程中，"冒烟测试"的主要目的是？
4. 单元测试、集成测试、系统测试中，集成测试的测试对象是？
5. 关于 Alpha 测试与 Beta 测试，以下描述正确的是？
6. 在 OSI 参考模型中，负责"建立、管理和终止通信会话"的是哪一层？
7. 在 OSI 参考模型中，物理层的主要功能是？
8. IPv4 地址的二进制长度是？
9. 在 IP 地址中，127.0.0.1 表示的是？
10. HTTP/3 相比 HTTP/1.1 和 HTTP/2，其底层传输协议改为？
11. HTTP 状态码 401 Unauthorized 表示？
12. HTTP 状态码 403 Forbidden 表示？
13. 在 Linux 中，执行 `chmod 755` 后，文件权限对应的含义是？
14. 在 Linux 中，用于创建软链接（符号链接）的命令是？
15. Linux 中，与 `ps`（一次性进程快照）相比，`top` 命令的特点是？
16. 在 Selenium 中，用于清空输入框已有内容的元素操作方法是？
17. 在 Selenium 中，`get_attribute()` 方法的作用是？
18. 在 Selenium 中，关于 close() 与 quit() 方法的区别，正确的是？
19. 在 JMeter 中，下列取样器中"不发送实际请求、用于调试查看变量值"的是？
20. 在 JMeter 线程组中，启用"调度器"设置持续运行时长时，循环次数应如何设置？
21. 在 Git 中，用于将远程仓库完整克隆到本地（含历史记录）的命令是？
22. 在 MySQL 中，用于删除整张表（含表结构与数据）的命令是？
23. Python 中，字符串 `"123456789"` 执行切片 `"123456789"[0:-1]` 后的结果是？
24. 关于 JDK、JRE、JVM 三者的包含关系，正确的是？
25. ES6 中，使用 `let` 声明的变量其作用域是？
26. 关于软件测试的分类，以下描述正确的有？（多选）
27. 关于 OSI 参考模型各层的功能，以下对应正确的有？（多选）
28. 关于 TCP/IP 四层模型，以下对应正确的有？（多选）
29. 在 Linux 中，以下哪些属于用户管理相关的命令？（多选）
30. 关于 Selenium 的浏览器操作，以下描述正确的有？（多选）
31. 关于 JMeter 线程组的属性，以下描述正确的有？（多选）
32. 关于 Python 的数据类型，以下描述正确的有？（多选）
33. 关于 MySQL 的登录与退出方式，以下描述正确的有？（多选）
34. 关于 Python 语言的特点，以下描述正确的有？（多选）
35. 关于 Java 语言，以下描述正确的有？（多选）
36. 按照 ISO 9000 的定义，质量是一组______特性满足要求的程度。
37. HTTP 状态码 502 Bad Gateway 表示______错误。
38. 在 MySQL 中，用于查看当前服务器中所有数据库列表的命令是 show ______。
39. 在 Selenium 中，用于刷新当前页面的方法是 driver.______()。
40. 在 MySQL 中，用于查看表结构的命令是______（填写命令格式，如"命令 表名"）。
41. Python 中，字符串 "abc" * 3 的运算结果是______。
42. Java 中，用于将 .java 源文件编译为 .class 字节码文件的命令是______。
43. ES6 中，const 声明的常量在初始化后______（填写"可以"或"不可以"）重新赋值。
44. 在 MySQL 中，用于指定使用某个数据库的命令是 ______（填写命令格式，如"命令 库名"）。
45. 在 Git 中，用于将本地提交推送到远程仓库的命令是 git ______。
46. 请简述软件缺陷管理（从缺陷发现到关闭）的完整流程，并列举常见的缺陷状态。
47. 请简述 HTTP 协议"无状态"特性的含义，并说明 Cookie 与 Session 在解决该问题时的作用与区别。
48. 请简述 Web 前端开发中 HTML、CSS、JavaScript 三者各自的作用与分工。
49. 请简述性能测试中常用的性能指标（并发量、响应时间、吞吐率、成功率）各自的含义。
50. 请简述 Java 面向对象的三大特性（封装、继承、多态）各自的含义，并分别举例说明。

## 2026-08-15
1. 在软件测试中，不运行被测程序，通过评审代码、需求文档或使用静态分析工具来发现缺陷的测试方式称为？
2. 按照 ISTQB 软件测试七大原则，同一组测试用例反复执行后"免疫"、难以再发现新缺陷的现象，被称为？
3. "缺陷聚集"原则（二八定律）的核心含义是？
4. "无谬误谬误"原则强调的是？
5. 测试用例八大要素中，"前置条件"描述的是？
6. 回归测试中，只执行与被修改代码相关的测试用例、而不执行全部用例的策略称为？
7. 需要深入代码内部逻辑、根据程序实现结构设计用例，能够获得较高覆盖率的测试方式是？
8. 在"多因素、多水平"参数组合极多的场景下，用最少的用例组合覆盖两两交互的设计方法是？
9. 在 Web 站点架构中，将图片、CSS、JS 等静态资源由 Web 服务器直接返回、把 /api 等动态请求转发给应用服务器处理的设计称为？
10. HTTP 请求头中，用于标识客户端浏览器类型、操作系统等信息的字段是？
11. Fiddler 作为独立抓包软件，它在 Windows 平台上抓取 HTTP 请求所采用的基本机制是？
12. 在 TCP/IP 网络中，通过已知 IP 地址查询对应 MAC 地址的协议是？
13. 关于企业内部网络（intranet）的特点，以下描述正确的是？
14. 在 Windows 命令提示符中，用于查看本机所有端口占用情况的命令是？
15. 在 Linux 中，用于显示当前系统时间，且可通过 +%Y 等格式参数定制输出格式的命令是？
16. 在 Linux 中，用于查看文件开头部分内容，默认显示前 10 行的命令是？
17. 在 CentOS 7 及之后的 Linux 系统中，用于管理服务（如 systemctl start/stop/restart/status）的命令是？
18. 在 Linux 中，用于改变文件或目录所有者（属主）的命令是？
19. 在 MySQL 中，删除表中所有记录、保留表结构，并同时重置自增计数（相当于重建一张空表）的命令是？
20. 在 MySQL 中，使用 limit 进行分页查询，查询第 3 页、每页 10 条记录时，开始索引应为？
21. 在 MySQL 查询中，用于去除查询结果中重复记录的关键字是？
22. 关于 MySQL 存储引擎 InnoDB 与 MyISAM 的区别，以下描述正确的是？
23. 在 Git 项目中，用于配置忽略提交规则（如忽略 .idea、target、*.iml 等文件）的文件是？
24. 当多人在 Git 协同开发中产生代码冲突，且需要与对方沟通后手动保留双方内容时，应选择？
25. 在 Selenium 中，元素可见但点击时被其他元素（如弹窗遮罩）拦截导致点击失败，抛出的异常是？
26. 关于等价类划分法，以下说法正确的有？（多选）
27. 关于测试金字塔与测试左移，以下说法正确的有？（多选）
28. 关于一个完整 Web 站点的组成，以下说法正确的有？（多选）
29. 关于 Linux 文件目录操作命令，以下说法正确的有？（多选）
30. 关于 MySQL 聚合函数，以下说法正确的有？（多选）
31. 在 MySQL 中，以下哪些属于常见的字段约束？（多选）
32. 关于 Git 分支，以下说法正确的有？（多选）
33. 关于 Selenium 异常处理，以下做法正确的有？（多选）
34. 关于 Python 标识符，以下说法正确的有？（多选）
35. 关于 Apifox 的鉴权（Auth）方式，以下说法正确的有？（多选）
36. 测试金字塔中，投入数量最多、处于金字塔底层的测试是 ______ 测试。
37. 一个完整的 HTTP 请求由请求行、请求头和 ______ 三部分组成。
38. Linux 中，用于显示当前登录用户自身名称的命令是 ______。
39. Linux 运行级别中，登录后进入图形界面（X11）GUI 模式的运行级别是 ______。
40. MySQL 模糊查询使用 like 关键字，其中匹配任意个任意字符的占位符是 ______。
41. MySQL 中，主键字段后加上 ______ 关键字即可实现自增长。
42. Git 中，使用 git remote ______ origin <远程仓库地址> 将本地仓库与远程仓库建立关联。
43. Selenium 中，页面刷新或 DOM 重建导致之前定位的元素引用失效时，抛出的异常是 ______Exception。
44. pytest 中，实现"同一用例用不同输入跑多次"的参数化装饰器是 @pytest.mark.______。
45. Python 中，用于读取 Excel（.xlsx）文件实现数据驱动的第三方库是 ______。
46. 请简述软件缺陷报告中"严重程度（Severity）"与"优先级（Priority）"的区别，并举例说明"严重程度高但优先级不高"或"严重程度低但优先级高"的典型场景。
47. 请简述软件测试七大原则中"测试显示缺陷存在，但不能证明无缺陷"与"无谬误谬误"各自的含义，并说明两者的区别。
48. 请简述 MySQL 中 where 与 having 在使用时机上的区别，并说明聚合函数（如 count、avg）在两者中的使用限制。
49. 请简述 Git 中工作区、暂存区、版本库三个区域的作用，并说明 git add、git commit、git push 三个命令各自完成的操作。
50. 请简述 Apifox 中"接口（API）、接口用例、测试场景、环境"四个概念的含义，并说明如何实现"用登录接口返回的 token 驱动后续接口"的请求串联。

## 2026-08-17
1. Python 中，可通过标准库的哪个模块查看当前版本的全部保留关键字？
2. Python 字符串前加 r 前缀（如 r"hello\nworld"）的作用是？
3. Python3 中，关于长整型 long 与整型 int 的关系，正确的是？
4. 关于 Python re 模块中 re.match 与 re.search 的区别，正确的是？
5. 正则表达式标志 re.M | re.I 中，re.I 的含义是？
6. Python 关键字 lambda 用于？
7. Python 中，is 与 == 的区别是？
8. MySQL 分组查询（group by）中，select 后允许出现的字段只能是？
9. MySQL 中 union 合并查询的默认行为是？
10. MySQL 唯一约束（unique）字段的取值特点，正确的是？
11. 关于 MySQL 外键约束，以下描述正确的是？
12. Linux 中 find / -name "*.log" 命令的作用是？
13. Linux 中，mkdir -p 参数的作用是？
14. Linux shell 特殊变量中，用于获取脚本参数个数的是？
15. Linux shell 中，上一条命令执行成功返回 0、失败返回非 0，用于获取该返回值的特殊变量是？
16. Linux 中 cut -d ":" -f 3 命令的作用是？
17. Git 中，git config --global user.name "xxx" 命令的作用是？
18. TCP 连接断开时的四次挥手，第一次挥手发送的报文段是？
19. 常见服务端口号中，SSH 远程登录默认使用的端口是？
20. HTTP 状态码 404 表示？
21. HTTP 状态码 304 Not Modified 表示？
22. HTTP 请求方法中，用于对资源进行"局部更新"的方法是？
23. IPv6 地址的二进制长度是？
24. Selenium 中，设置全局隐式等待（元素查找最长等待时间）的方法是？
25. Selenium 中，通过 <input type="file"> 标签实现文件上传的标准做法是？
26. 以下属于 Python 循环控制关键字的有？（多选）
27. 关于 Python 字符串，以下说法正确的有？（多选）
28. 关于 Python re 模块，以下说法正确的有？（多选）
29. 关于 MySQL 外键约束与表关系，以下说法正确的有？（多选）
30. 关于 MySQL union 合并查询，以下说法正确的有？（多选）
31. 关于 Linux 文本处理命令，以下对应正确的有？（多选）
32. 关于 Linux shell 特殊变量，以下说法正确的有？（多选）
33. 以下属于 HTTP 常见请求方法的有？（多选）
34. 关于 Selenium 隐式等待与显式等待，以下说法正确的有？（多选）
35. 关于 TCP 四次挥手，以下说法正确的有？（多选）
36. Python 中，单行注释以 ______ 开头。
37. Python 中，表示"空值 / 无值"的关键字是 ______。
38. Python re 模块中，用于替换字符串中匹配项的函数是 re.______。
39. 正则匹配对象的方法中，groups() 返回包含所有小组字符串的 ______（填写数据类型）。
40. MySQL 合并查询中，union ______ 表示不去重地合并两个查询结果。
41. MySQL 中，删除外键约束的命令格式为 alter table 从表名 drop ______ 外键名。
42. Linux shell 特殊变量中，用于获取脚本名称的变量是 ______。
43. Linux 中，用于按文件名查找文件或目录的命令是 ______。
44. Git 中，用于设置全局用户信息（用户名 / 邮箱）的命令是 git ______。
45. Selenium 中，设置全局隐式等待、超时后抛出异常的方法是 driver.______(10)。
46. 请简述 Python 中 re.match 与 re.search 的区别，并说明匹配对象的 group() 与 groups() 方法各自返回什么。
47. 请简述 MySQL 中主键约束与外键约束的区别，并说明"多对多"关系在数据库中通常如何实现（需要几张表、外键如何设置）。
48. 请简述 TCP 四次挥手释放连接的过程（四次挥手各次发送什么报文段），并说明为什么断开连接需要四次而不是三次。
49. 请简述 Linux shell 特殊变量 $#、$?、$*、$@、$0 各自的含义。
50. 请简述 Selenium 中隐式等待（implicitly_wait）与显式等待（WebDriverWait + expected_conditions）的区别，并说明各自适用的场景。

## 2026-08-18
1. 既关注软件内部结构，又关注外部功能表现，常见于接口测试的测试方式是？
2. ISO 25010 质量模型中，描述"系统在异常或故障时能否容错、恢复"的特性是？
3. 在 V 模型基础上，强调测试与开发并行、需求文档阶段即开始评审的测试模型是？
4. 按业务流程拆分成基本流与备选流，沿路径设计测试用例的方法是？
5. HTTP 状态码 301 Moved Permanently 表示？
6. HTTP 状态码 500 表示？
7. UDP 头部只有 8 字节，与之相比 TCP 头部的最小大小是？
8. 常见服务端口号中，FTP 文件传输协议默认使用的端口是？
9. TCP/IP 应用层协议中，用于将域名解析为 IP 地址的协议是？
10. Python 中 `[x**2 for x in range(5)]` 这种快速生成列表的写法称为？
11. Python 中关于 True 与整数 1 的关系，正确的是？
12. Python 中，9 // 2 的运算结果是？
13. Python 3.8 新增、可在表达式中同时完成赋值并返回值的运算符是？
14. MySQL 中，`select 字段列表 from 表1 left outer join 表2 on 条件` 的查询结果是？
15. MySQL 子查询的查询结果是多行单列时，应配合使用的运算符是？
16. MySQL 中，用于限定字段值不能为空的约束是？
17. Selenium 中，`find_element(By.CSS_SELECTOR, 'div#form input#kw')` 表示定位？
18. Selenium 中，需要按链接的"部分文本"模糊匹配定位 <a> 链接，应使用？
19. Selenium 中，用于对当前页面截图保存的方法是？
20. 在 Git 中，将 dev 分支的修改合并到当前 master 分支，应执行的命令是？
21. Linux 运行级别中，"单用户工作状态，root 权限，用于系统维护，禁止远程登录"的级别是？
22. Linux 中，`shutdown -h now` 的作用与下列哪个命令等价？
23. JMeter 中，用于汇总统计所有请求的响应时间、吞吐量、错误率等指标的监听器是？
24. 与 C/S 架构相比，B/S 架构最突出的特点是？
25. 对于具有明显状态机特征的模块（如订单状态流转、审批流程），最适合的测试用例设计方法是？
26. Python 中，以下哪些属于不可变数据类型？（多选）
27. Python 中，以下哪些属于列表（list）的常用方法？（多选）
28. MySQL 中，以下哪些属于多表连接查询的类型？（多选）
29. TCP 保证可靠传输的机制包括以下哪些？（多选）
30. Selenium 中，以下哪些属于对元素进行操作的方法？（多选）
31. Linux 中，以下哪些属于关机或重启系统的命令？（多选）
32. JMeter 中，以下哪些属于取样器（Sampler）？（多选）
33. ISO 25010 质量模型中，以下哪些属于非功能特性？（多选）
34. MySQL 中，以下哪些属于条件查询的关键字？（多选）
35. Git 中，分支合并的方式包括以下哪些？（多选）
36. Python 中，使用了 ______ 关键字的函数被称为生成器函数。
37. Python 3.6 引入的格式化字符串字面值（f-string），以字母 ______ 作为前缀。
38. MySQL 中，左外连接的关键字是 left ______ join。
39. MySQL 中，用于限定字段值不能为空的约束是 ______。
40. Selenium 中，获取元素可见文本内容的属性是 element.______。
41. HTTP 状态码 200 表示请求 ______。
42. 常见服务端口号中，FTP 默认使用的端口是 ______。
43. Linux 中，将缓冲区中的数据立即同步写入磁盘的命令是 ______。
44. Git 中，将指定分支合并到当前分支使用的命令是 git ______。
45. UDP 协议的头部固定大小为 ______ 字节。
46. 请简述黑盒测试与白盒测试的主要区别，并各举一个典型应用场景。
47. 请简述 ISO 25010 质量模型中"可靠性"特性的核心关注点，并与"可维护性"特性进行区分。
48. 请简述 Python 中列表（list）与元组（tuple）的主要区别，并说明各自适用的场景。
49. 请简述 MySQL 内连接、左外连接、右外连接三者查询结果的区别，并举例说明左外连接的典型应用。
50. 请简述 Selenium 中 find_element 与 find_elements 方法的区别，并说明使用 find_elements 的典型场景。

## 2026-08-19
1. 软件测试的狭义经典定义强调，测试的核心目的是？
2. 在软件测试的验证与确认（V&V）中，"Are we building it right?（我们是否正确地构建了产品）"描述的是哪一类活动？
3. 在常见软件测试模型中，强调"测试活动独立成一条流，只要就绪即可尽早、迭代执行"的模型是？
4. 测试左移（Shift Left）的核心思想是？
5. 在通用的缺陷管理体系中，缺陷的"严重程度（Severity）"通常分为几级？
6. 以下关于缺陷"优先级（Priority）"的描述，正确的是？
7. 在软件测试中，SQL 注入、XSS 跨站脚本、越权访问等测试通常归类于？
8. 以下测试中，属于非功能测试的是？
9. 在软件测试的阶段划分中，集成测试的主要测试依据是？
10. 在测试过程的四个基本活动（计划 / 设计 / 执行 / 评估）中，"分析测试结果、编写测试报告、评估质量"属于？
11. 关于静态测试（如需求 / 设计 / 代码评审、静态分析）相较于动态测试，正确的说法是？
12. 关于敏捷测试的实践，以下说法不正确的是？
13. 数据库事务的 ACID 特性中，"一致性（Consistency）"指的是？
14. 关于数据库索引（Index）的作用，以下描述正确的是？
15. 数据库设计的第一范式（1NF）要求的是？
16. 在关系型数据库中，"视图（View）"的本质是？
17. 在 Git 中，当你需要临时保存当前未提交的修改、切换到其他分支处理紧急任务，事后再恢复这些修改，应使用的命令是？
18. 在 Git 中，git reset --hard 与 git reset --soft 的主要区别是？
19. 在 Linux 中，文件权限数字 644 表示？
20. 在 Linux 中，创建硬链接（不使用 -s 参数）使用的命令是？
21. 在 Linux 中，既能把命令输出显示到屏幕、又能同时写入文件的命令是？
22. 在 Linux 中，用于测试与目标主机的网络连通性、基于 ICMP 回显请求的最常用命令是？
23. 在 Python 中，用于在不修改原函数代码的前提下扩展函数功能的语法机制是？
24. 在 Python 异常处理中，无论是否发生异常、都一定会执行的代码块关键字是？
25. 在 Python 中，以"追加"模式打开文件（文件不存在则创建、存在则在末尾追加内容）使用的模式字符是？
26. 从"是否关注软件内部"的维度，软件测试可以分为以下哪些类别？
27. 以下属于非功能测试的有？
28. 软件测试按测试阶段通常分为以下哪些？
29. 以下属于黑盒测试用例设计方法的有？
30. 以下属于白盒测试逻辑覆盖级别的有？
31. 以下属于 MySQL 中常见索引类型的有？
32. 在 Git 中，以下哪些命令常用于"撤销 / 回退"或暂存未提交的修改？
33. 在 Linux 文件权限表示中，以下说法正确的有？
34. 在 Python 中，以下属于可变（mutable）数据类型的有？
35. 关于 HTTP 请求方法，以下说法正确的有？
36. 在 MySQL 中，用于提交事务、使已执行的事务修改永久生效的命令是 ______。
37. 数据库设计第三范式（3NF）要求在满足第二范式（2NF）的基础上，进一步消除 ______ 依赖。
38. 在 MySQL 中，"字段值必须唯一且不允许为 NULL、并且一张表只能有一个"的索引类型是 ______ 索引。
39. 在 Git 中，用于给某次提交打上版本标签（如 v1.0.0）的命令是 git ______。
40. 在 Git 中，用于查看工作区、暂存区与版本库之间文件差异的命令是 git ______。
41. 在 Linux 中，将一条命令的标准输出既显示在屏幕上、又写入指定文件的命令是 ______。
42. 在 Python 中，使用 `@` 符号定义、用于在不修改原函数代码前提下扩展函数功能的语法结构称为 ______。
43. 在 Python 异常处理中，无论是否发生异常都会执行的代码块关键字是 ______。
44. 在 Selenium 中，处理标准 `<select>` 下拉框元素需要使用的类是 ______。
45. 在 JMeter 中，用于模拟用户"思考时间"、控制请求之间时间间隔的元件类型是 ______。
46. 请简述移动端 App 专项测试通常包含哪些维度（如安装卸载、耗电量、流量、弱网、兼容性、稳定性等），并说明各自关注的核心点。
47. 请简述接口测试中 REST 风格的主要特征（资源 URL、HTTP 方法、无状态等），并说明它与 SOAP 的主要区别。
48. 请简述 Python 中迭代器（iterator）与生成器（generator）的区别，并说明实现一个自定义迭代器必须定义哪两个方法。
49. 请简述 Linux 中硬链接与软链接（符号链接）的区别，并分别给出创建这两种链接的命令。
50. 请简述软件测试中探索性测试（Exploratory Testing）的特点，并说明它与脚本化测试（严格按预先写好的用例执行）的主要区别。

## 2026-08-20
1. 在 Selenium 处理极验类滑块验证码时，引入"页面缩放比例 factor"主要是为了解决什么问题？
2. 模拟真人拖动滑块时，将轨迹设计为"先加速后减速、末尾轻微过冲并叠加随机抖动"的主要目的是？
3. 网站通过检测 window.navigator.webdriver 识别自动化，在 Selenium 中借助 CDP 在每次新文档加载前注入脚本去除该特征应使用哪条命令？
4. 在 Python 自动化识别"文字点选类"验证码时，常用于本地轻量 OCR 识别的库是？
5. 使用 pytest-xdist 插件实现用例多进程并行执行时，自动分配 worker 数量的命令参数是？
6. 在 Selenium 并发测试中，往往优先选择"多进程"而非"多线程"的主要原因是？
7. 在并发自动化测试中，为避免多个并发用例相互干扰，每个并发进程/线程应当？
8. pytest-xdist 中，希望某些用例强制在同一 worker 上串行执行应使用哪个标记？
9. 通过 window.performance.timing 计算"页面完全加载总耗时"的常用公式是？
10. Google Core Web Vitals 中，用于衡量"最大内容绘制"的指标是？
11. Core Web Vitals 对 LCP 的"良好"阈值要求是？
12. 衡量页面视觉稳定性、反映元素意外位移程度的 Core Web Vitals 指标是？
13. 在 Selenium 中获取 Chrome 底层性能日志通常借助？
14. 在页面运行中实时监听并采集 Web Vitals 指标变化，现代前端通常使用的 API 是？
15. 在 Apifox 后置脚本中，获取当前脚本所处事件名称（如 pre_request / test）的属性是？
16. 在 Apifox 前置脚本中，需要主动调用另一个接口（如先获取 token）应使用的 API 是？
17. 在 Apifox 变量体系中，局部变量（pm.variables 设置）的优先级通常？
18. 在 Apifox 后置脚本中，将响应体解析为 JSON 对象应使用？
19. 在 Apifox 后置脚本中断言 HTTP 状态码等于 200 可使用的表达式是？
20. 在 Apifox 脚本中，读取响应 Set-Cookie 中某个 cookie 的值应使用？
21. 使用 Python mysql-connector 建立与 MySQL 的连接，核心代码是？
22. 在 mysql-connector 中，执行一条 SQL 查询并获取结果集的标准流程是？
23. 使用 mysql-connector 批量插入多条记录应使用？
24. 在 mysql-connector 中，防止 SQL 注入的正确做法是？
25. 在 mysql-connector 执行 INSERT/UPDATE/DELETE 后，使修改落库必须调用？
26. 以下哪些属于 SVN 的常用命令？（多选）
27. 在 Android 系统中，以下属于"四大组件"的有？（多选）
28. JMeter 中，以下哪些属于"定时器"元件？（多选）
29. 在 Postman / Apifox 中，变量作用域通常包含哪些层级？（多选）
30. 关于使用 Newman 在命令行执行 Postman 集合，以下说法正确的有？（多选）
31. 在 Selenium 并发测试中，常见问题或需要注意的事项包括？（多选）
32. 以下哪些属于常见的前端页面性能监控与采集手段？（多选）
33. 使用 JMeter 以非 GUI 模式运行并生成 HTML 报告，涉及哪些参数？（多选）
34. 在 Python mysql-connector 中，以下哪些属于 cursor 对象的方法/属性？（多选）
35. 关于 SVN 与 Git 的区别，以下说法正确的有？（多选）
36. 在 Selenium 中使用 CDP 去除自动化特征时，常用命令是 driver.execute_cdp_cmd("Page.______", {...})，该方法会在每个新文档加载前注入脚本。
37. pytest-xdist 实现多进程并行的命令参数格式为 pytest -n ______。
38. 在 window.performance.timing 中，从导航开始到页面 load 事件结束的总耗时可表示为 ______ - navigationStart。
39. Google Core Web Vitals 中，FID 的"良好"阈值是小于 ______ 毫秒。
40. 在 Apifox 脚本中，主动发起一个 HTTP 请求（常用于前置获取 token）的方法是 pm.______()。
41. 使用 Python mysql-connector 时，获取查询结果所有行的方法是 cursor.______()。
42. 在 mysql-connector 中，执行批量插入应使用 cursor.______(sql, data_list) 方法。
43. SVN 中，撤销本地未提交修改、恢复到版本库状态的命令是 svn ______。
44. SVN 中，从版本库首次获取工作副本的命令是 svn ______。
45. 在 Postman 生态中，用于从命令行批量运行集合并集成到 CI 的官方工具是 ______。
46. 请简述在 Selenium 中处理滑块验证码（如极验）的基本思路，至少包括：如何识别缺口位置、如何计算实际拖动坐标、如何构造拟真拖动轨迹。
47. 请简述 pytest-xdist 多进程并行与 ThreadPoolExecutor 多线程并发在执行 Selenium 用例时的主要区别，并说明为何常优先选择多进程。
48. 请简述 Google Core Web Vitals 三大指标（LCP、FID/INP、CLS）各自衡量的内容，以及各自"良好"的阈值标准。
49. 请简述使用 Python mysql-connector 操作 MySQL 的标准流程，并说明 %s 占位符防止 SQL 注入的原理。
50. 请简述 SVN 与 Git 在"版本号机制"和"离线工作能力"上的主要区别，并说明 SVN 冲突后常见解决步骤。

## 2026-08-22
1. 白盒测试中，"判定-条件覆盖"要求同时满足以下哪两类覆盖？
2. 在软件测试度量中，"缺陷密度（Defect Density）"通常定义为？
3. 验收测试（Acceptance Testing）通常由谁主导或参与执行？
4. 探索性测试中"基于会话的测试管理（SBTM）"的核心组织单位是？
5. 在测试替身分类中，用于验证"某个方法是否被调用、以及调用时的参数"的对象是？
6. 持续测试（Continuous Testing）在 DevOps 交付流水线中的定位是？
7. 消费者驱动的契约测试（Consumer-Driven Contract Testing）常用实现工具是？
8. 模糊测试（Fuzz Testing）主要用于发现哪类问题？
9. 在安全测试中，攻击者诱导已登录用户在不知情的情况下提交非预期请求的攻击方式是？
10. 性能测试中"思考时间（Think Time）"模拟的是？
11. LoadRunner 中负责脚本录制与开发的组件是？
12. 性能测试"拐点法"主要用于确定？
13. JMeter 中通过正则表达式从响应中提取动态值的后置处理器是？
14. Selenium 4 引入的"相对定位"特性（如 above、toLeftOf、near）称为？
15. 在 Page Object 模式中使用 @FindBy 配合 PageFactory.initElements 完成元素懒加载初始化的方式属于？
16. Appium 服务端与被测端驱动之间通信所依赖的基础协议是？
17. Android 测试中用于实时查看设备运行日志、过滤崩溃信息的命令是？
18. 在 ADB 命令中，用于将本地电脑文件推送到 Android 设备指定路径的命令是？
19. RESTful 设计中，客户端成功删除资源、服务端无返回内容时常返回的状态码是？
20. 与传统的服务器端 Session 认证相比，JWT 的主要优势在于？
21. MySQL 四个事务隔离级别中，能完全避免脏读、不可重复读和幻读的是？
22. Redis 缓存问题中，"缓存穿透"指的是？
23. Docker 运行容器时，将容器端口映射到宿主机端口使用的参数是？
24. Kubernetes 中作为最小部署与调度单元、可包含一个或多个容器的是？
25. Jenkins 中用于以代码形式定义构建、测试、部署流水线的文件是？
26. 在典型 CI/CD 流水线中，以下哪些阶段通常属于软件自动交付流程？（多选）
27. 在基于风险的测试中，应优先安排测试资源的模块通常具有哪些特征？（多选）
28. 以下哪些属于常见的测试替身（Test Double）？（多选）
29. 在自动化测试框架设计中，以下哪些是常见的驱动模式？（多选）
30. JMeter 中，以下哪些属于逻辑控制器（Logic Controller）？（多选）
31. 在 Appium 移动端自动化中，常用的元素定位方式包括以下哪些？（多选）
32. Python 测试生态中，以下哪些属于常用测试或打桩工具？（多选）
33. 在微服务架构下，为保障接口质量常用以下哪些测试策略？（多选）
34. 关于 Docker 镜像与容器，以下说法正确的有？（多选）
35. 关于 Redis 缓存相关问题与机制，以下说法正确的有？（多选）
36. 将发现的缺陷按"发生可能性 × 影响程度"评估并据此安排测试优先级的测试方法称为 ______ 测试。
37. 在敏捷开发中，用 Given-When-Then 结构编写验收标准、由业务/开发/测试三方达成共识的实践称为 ______。
38. 在 HTTP 协商缓存机制中，服务端使用 ______ 响应头返回资源的唯一标识，供后续请求校验是否变化（配合 304）。
39. 在 MySQL 中，查看一条 SQL 的执行计划、判断是否走索引，应使用的命令关键字是 ______。
40. Redis 支持的持久化机制主要包括 RDB 快照与 ______ 两种。
41. 在 Linux 中，用于查看网络连接、监听端口等信息的现代命令（常作为 netstat 的替代）是 ______。
42. 在 Docker 中，用于查看容器实时日志输出的命令是 docker ______。
43. 在 Kubernetes 中，用于声明式地将期望状态同步到集群资源的命令是 kubectl ______。
44. Selenium 4 中，能够自动下载并管理浏览器驱动的组件称为 Selenium ______。
45. 在性能测试中，单位时间内系统成功处理的事务数量指标称为 ______（如 TPS）。
46. 请简述"测试右移（Shift-Right）"的含义，并列举在生产环境侧保障质量的常用手段。
47. 请简述"测试替身（Test Double）"中 Dummy、Stub、Mock、Fake、Spy 五种对象的区别，并说明各自典型使用场景。
48. 请简述持续集成（CI）与持续交付（CD）/持续部署的区别，并说明自动化测试在其中的作用与嵌入位置。
49. 请简述 OWASP Top 10 中至少五种常见 Web 安全漏洞，并分别说明其防御要点。
50. 请简述可观测性三大支柱（日志、指标、链路追踪）各自关注什么，并举例说明常用开源工具。
